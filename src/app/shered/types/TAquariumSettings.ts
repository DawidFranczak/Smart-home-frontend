export type TAquariumSettings = {
    color:string,
    fluoMode:boolean,
    fluoStart:string,
    fluoStop:string,
    id:number,
    ledMode:boolean,
    ledStart:string,
    ledStop:string,
    mode:boolean,
    name:string
}